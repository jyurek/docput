<%= form_for @changeset, layouts_path(@conn, :create), [], fn(form) -> %>
  <%= render "_form.html", form: form %>
  <div class="submit">
    <button><%= gettext("Create Layout") %></button>
  <div>
<% end %>
